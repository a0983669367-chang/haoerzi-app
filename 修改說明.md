# 康和「好日子」App — 使用與修改說明

> 銀髮退休現金流管家　·　康和證券實習專題原型

---

## 一、怎麼打開？

**直接用滑鼠雙擊 `index.html`** 就會用瀏覽器開啟，不需要安裝任何東西、不需要架伺服器。

> 這份程式碼刻意避開了 ES Module、`fetch()` 等會被瀏覽器安全機制擋下的寫法，
> 所以不會出現「本機檔案打不開」或「畫面空白」的問題。

⚠️ 唯一限制：離線快取（Service Worker）在雙擊開啟時不會啟動，這是正常的。
部署到網路上之後就會自動生效。

---

## 二、資料夾結構：我想改東西，該動哪個檔案？

```
康和好日子App/
├── index.html          ← 網頁骨架（很少需要改）
│
├── css/
│   └── styles.css      ← 【改外觀】顏色、字級、間距、手機版樣式
│
├── js/
│   ├── data.js         ← 【最常改】所有數字與文字內容
│   ├── screens.js      ← 【改版型】各畫面的排版結構
│   ├── notes.js        ← 【改講稿】右側簡報說明文字
│   └── app.js          ← 程式邏輯（通常不用動）
│
├── assets/icons/       ← App 圖示（192 / 512 / maskable）
├── manifest.json       ← PWA 設定：App 名稱、圖示、主題色
├── service-worker.js   ← 離線快取
├── 產生單檔.py          ← 把上面全部合併成一支 HTML 的工具
├── 單檔版本_備份.html   ← ⚠️ 自動產生，不要直接改（見下方說明）
└── 修改說明.md          ← 你正在看的這份
```

> **關於 `單檔版本_備份.html`**
> 它是把 `index.html` + `css/` + `js/` 全部合併成一支檔案的版本，方便**直接寄給別人**
> 或在沒有網路的電腦上雙擊開啟（不需要資料夾、不需要伺服器）。
>
> ⚠️ **它是產生出來的結果，不是原始碼。** 改內容請一律改 `js/data.js`，
> 改完之後在這個資料夾裡執行一次就會同步：
>
> ```
> python3 產生單檔.py
> ```

---

## 三、常見修改對照表

| 我想改… | 打開這個檔案 | 找這個地方 |
|---|---|---|
| 配息金額、入帳項目 | `js/data.js` | `PAYS` |
| 新增一筆入帳（月曆自動長出金點） | `js/data.js` | `PAYS` 加一行 |
| 總資產、年齡、報酬率、通膨率 | `js/data.js` | `CONFIG` |
| 客戶風險屬性、可承作 RR 上限 | `js/data.js` | `CONFIG.riskProfile`、`CONFIG.maxRR` |
| 月退俸目標的滑桿範圍 | `js/data.js` | `PENSION` |
| 補缺口的三個方案 | `js/data.js` | `SOLUTIONS` |
| 投資商品清單、分類 | `js/data.js` | `PRODUCTS`、`PRODUCT_CATS` |
| ETF 的五個小分類（市值型／高股息／主動式…） | `js/data.js` | `ETF_SUBS`（商品用 `sub` 欄位對應） |
| 代銷基金的小分類（KBI／寶盛） | `js/data.js` | `FUND_SUBS` |
| 分類標題下的白話解釋 | `js/data.js` | `CAT_DESC` |
| RR1–RR5 白話對照（④ 卡片＋ AI 說明共用） | `js/data.js` | `RR_LEVELS` |
| 手機掃描 QR code 的網址 | `js/data.js` | `DEPLOY.url`（改完要重跑 `產生QRcode.py`） |
| AI 的常見問題與回答 | `js/data.js` | `AI_QUICK`、`AI_ANSWERS` |
| AI 主動提醒的內容 | `js/data.js` | `AI_PUSH` |
| 語音聽到什麼字對應哪一題 | `js/data.js` | `VOICE_MATCH` |
| 各類商品的費用說明 | `js/data.js` | `CAT_FEE` |
| 申購試算滑桿的金額範圍 | `js/data.js` | `ORDER` |
| 通用風險揭露文字（五個客戶畫面＋桌機頁尾共用） | `js/data.js` | `GENERAL_DISCLAIMER` |
| 康和專區全部內容（電話、網址、防詐關鍵字、快訊） | `js/data.js` | `CONCORDS`、`CZ_ICONS` |
| ① 兩個子頁（月曆／老本試算）的名稱 | `js/data.js` | `CASH_SUBS` |
| 底部導覽的五格 | `js/data.js` | `SCREENS_CLIENT`、`NAV_ICONS` |
| 吉祥物「小方」的台詞 | `js/data.js` | `MASCOT` |
| 吉祥物長相（純 SVG） | `js/screens.js` | `S.mk()` |
| 營業員的客戶名單與訊號 | `js/data.js` | `CLIENTS` |
| 營業員姓名、分公司、管理戶數 | `js/data.js` | `ADVISOR` |
| 客戶詳情的行為時間軸 | `js/data.js` | `CLIENT_TIMELINE` |
| 字級三段的放大倍率 | `js/data.js` | `FONT_STEPS` |
| 未來三個月預估 | `js/data.js` | `FORECAST` |
| **整套配色** | `css/styles.css` | 最上方 `:root` |
| 字級大小 | `css/styles.css` | 搜尋 `font-size` |
| 手機版全螢幕行為 | `css/styles.css` | 第 25 區 |
| 右側簡報講稿 | `js/notes.js` | `NOTES` |
| 某個畫面的排版 | `js/screens.js` | `S.畫面名稱` |
| App 名稱、圖示、主題色 | `manifest.json` | — |

### 範例 1：新增一筆「中華電信股利」入帳

打開 `js/data.js`，在 `PAYS` 裡加一行：

```js
var PAYS = {
  5:  [{ n:"勞保老年年金", v:18200, d:"每月 5 日入帳" }],
  12: [{ n:"中華電信股利", v:12000, d:"持有 5,000 股" }],   // ← 新增這行
  ...
};
```

存檔、重新整理瀏覽器 → 月曆 12 號自動出現金色圓點，總金額也會自動重算。
（`TOTAL` 是程式自動加總的，不用手動改。）

### 範例 2：換掉整站的主色

打開 `css/styles.css`，改最上面 `:root` 裡的這兩行：

```css
--navy:#DB190E;      /* 主色，改成你要的顏色 */
--navy-dk:#9E120A;   /* 同色系壓深一階，給漸層與 hover 用 */
```

全站的標題文字、色條、主要按鈕、選中的頁籤會一起變色。
換色之後記得順手確認一下白字在新顏色上的對比度有沒有到 4.5:1，
`manifest.json`／`index.html` 的 `theme-color` 也要跟著改，
再執行一次 `python3 產生App圖示.py` 重繪 App 圖示。

### 範例 3：新增一檔投資商品

打開 `js/data.js`，在 `PRODUCTS` 裡加一筆：

```js
{ c:"fund", nm:"新興市場債券基金", rr:4, y:0.068, min:"3 萬", pay:"每月配息",
  d:"配息率高，但須留意匯率與信用風險。" },
```

- `c` 是分類代號：`etf` ETF／`fund` 基金／`sn` 股權利率連結／`fi` 固定收益／`ins` 保險／`etc` 其他
- `y` 是年化配息率，程式會自動換算成「每投入 100 萬，每月約領 X 元」
- `rr` 超過 `CONFIG.maxRR` 時，卡片會自動標示「⚠ 需加強評估後方可承作」
- 加 `warn:true` 可讓卡片外框變成警示色（用於不保本商品）
- 存檔後不用另外寫 AI 問答——④ 每張卡下面的「問小幫手」會自動從這幾個欄位組出說明

如果是 `c:"etf"`，要多加一個 `sub` 欄位指定是哪個小分類（見 `ETF_SUBS`）：

```js
{ c:"etf", sub:"cap", nm:"元大台灣50（0050）", rr:3, y:0.032, min:"每月 3,000", pay:"每半年配息",
  d:"追蹤台灣市值前 50 大公司，最具代表性的大盤型 ETF，波動跟大盤同步。" },
```

`sub` 有五個值：`cap` 市值型／`div` 高股息／`bond` 債券型／`th` 主題海外／`active` 主動式。
代銷基金（`c:"fund"`）也有小分類，見 `FUND_SUBS`（`kbi`／`baer`）。
不配息的商品（`y:0`）預設會顯示「保障型商品，不以配息計算」，
如果那句話不適用（例如成長型 ETF 不配息但也不是保障型），
可以加一個 `altTx` 欄位覆蓋掉：

```js
altTx:"沒有固定配息，獲利來源是淨值上漲的價差，波動也比高股息型 ETF 大許多。"
```

---

## 三之二、這一版改了什麼（v2）

| 動作 | 原本 | 現在 | 為什麼 |
|---|---|---|---|
| 刪除 | ④ 防詐守門員 | — | 券商無權攔阻客戶出金（屬檢警警示帳戶機制），且無收入 |
| 刪除 | ⑤ 家庭共管 | — | 子女檢視他人帳戶牽涉個資法與帳戶本人使用原則，需信託或輔助宣告架構才可行 |
| 置換 | ② 三桶水配置 | ② 我的月退俸 | 三桶水只「描述」現況，不會產生交易；缺口才會 |
| 新增 | — | ④ 投資商品 | 對齊可承作業務：代銷基金、股權／利率連結、固定收益、保險、其他核准商品 |
| 新增 | — | ⑤ AI 小幫手 | 查現金流、翻譯術語、主動提醒，並蒐集客戶意圖訊號給營業員 |

**核心變現邏輯：** 目標月退俸 − 目前入帳 ＝ 缺口 → 缺口 × 12 ÷ 年配息率 ＝ 需投入本金 → 對應三個可下單的方案。
滑桿拉高，需投入本金同步放大——客單價與目標金額直接連動。

### v3 再補上的四件事

| 功能 | 在哪裡 | 解決什麼 |
|---|---|---|
| **月曆模擬** | ② 按「選這個 → 看月曆會變怎樣」 | 客戶看到的是「補完之後的樣子」，不是報酬率。成交前的臨門一腳 |
| **申購動線** | ④ 每張商品卡的「試算並申購」 | 把商品頁從型錄變成有動線，並演出手續費收入怎麼發生；含二次確認防誤觸 |
| **營業員視角** | 最上方身分切換 | 回答「這個 App 對康和賺什麼」——客戶行為變成帶金額的名單 |
| **無障礙** | 標題列「A 字級」、⑤ 麥克風與朗讀 | 落實「無障礙是預設，不是設定選項」這個主張 |

（v3 之後、v4 之前，另外還加上了品牌 logo、吉祥物「小方」、把 ③ 老本試算併進 ① 月曆的分頁、
以及 ③ 康和專區——把下單 App、投顧網站、樂齡專線、話術比對式防詐工具收在一起，
細節見「常見修改對照表」裡對應的 `CONCORDS`、`MASCOT`、`CASH_SUBS` 幾列。）

### v4 再補上的四件事

| 功能 | 在哪裡 | 解決什麼 |
|---|---|---|
| **ETF 獨立分類＋四個小分類** | ④ 商品分類籤新增「ETF」，內部依市值型／高股息／債券型／主題海外分組 | ETF 現在很熱門，但一堆代號長輩分不清差在哪；先分好類再挑 |
| **分類白話解釋＋商品自動問答** | ④ 每個分類標題下一句白話說明；每張商品卡下面「問小幫手」點了直接跳 ⑤ 問這一檔 | 商品說明從 `PRODUCTS` 資料自動組出來（`productAnswer()`），加商品不用手動多寫問答 |
| **RR1–RR5 白話對照卡** | ④ 頁面最上方 | 不用再自己猜「風險等級 4」是什麼意思，同一份文字 AI 說明也在用（v5 改成預設收合，見下方） |
| **手機掃描 QR code** | 桌機右側講稿正上方 | 簡報現場給人掃一下就能用手機看，不用打網址（v5 加了放大功能，見下方） |

---

### v5 再補上的兩件事

| 功能 | 在哪裡 | 解決什麼 |
|---|---|---|
| **QR code 可以點開放大** | 點右側小卡，開一個大浮層（點背景空白處／✕／Esc 都能關） | 原本的小卡在投影幕上太小，台下的人掃不到；放大版另外用了更高解析度的圖，不會糊 |
| **RR1–RR5 說明卡改成預設收合** | ④ 頁面最上方，用原生 `<details>`，點標題才展開 | 原本五行字一直展開會把下面的商品清單往下推，長輩要多滑一頁才看得到商品 |

**QR code 的維護方式跟康和 logo 一樣**：圖片是 base64 內嵌在 `css/styles.css`
的 `QR:BEGIN` / `QR:END` 兩行註解中間（小卡跟放大版共用同一張圖），
不是外部圖檔，單檔版本才不會有外部參照。網址只有一個地方要改（`data.js` 的 `DEPLOY.url`），
改完執行 `python3 產生QRcode.py` 重新產生圖片（第一次要先 `pip3 install qrcode[pil]`）。

> **關於「手機外框變大了」**：檢查過 `.phone`／`.screen` 的 CSS 數值，
> 這次改動完全沒有動到（392×768，跟一開始一樣）。用同一個瀏覽器視窗大小
> 比對改動前後的畫面，手機外框的像素大小是一致的。如果畫面上看起來偏大，
> 多半是瀏覽器頁面縮放被調高了（按 <kbd>Cmd</kbd>+<kbd>0</kbd> 重設回 100%），
> 不是原型本身的問題。

---

### v6：主色調換成康和紅，商品換成康和真的有代銷的

| 動作 | 內容 |
|---|---|
| **主色從藍換成紅** | `css/styles.css` 的 `--navy`／`--navy-dk` 兩個變數換成康和品牌紅（沿用變數舊名字，改色風險最低）|
| **警示色跟品牌紅拉開** | `--red` 從原本偏紅橘的 `#C0392B` 改成偏紫的酒紅色 `#7A2233`。主色已經是紅的之後，警示如果也用同一種紅，長輩會分不清「這是按鈕」還是「這是警告」——色相拉開之後兩者一眼就分得出來，順便把對比度也拉高到 9.97:1 |
| **logo 完全沒動** | 康和 logo（`.page-logo`／`.bar-logo`）是原本就內嵌的真實紅黑版 logo 圖檔，這次沒有再動，符合「logo 保持原本配色」的要求 |
| **PWA 圖示、theme-color 一起換** | `manifest.json`、`index.html` 的 `theme-color`、`assets/icons/` 三張圖示背景全部同步換成新紅色，新增 `產生App圖示.py` 負責重繪圖示（圖案本身不變，只換底色）|
| **代銷基金換成康和真的有代銷的** | 原本三檔基金是隨便編的示意名字，換成 **KBI 環球基金**、**寶盛全球優選組合**——這兩家真的是康和證券投資顧問擔任台灣總代理的境外基金公司（2006 年引進 KBI、2009 年引進瑞士寶盛），做這版之前特地查證過 |
| **ETF 加主動式子分類** | 原本 ETF 只有市值型／高股息／債券型／主題海外四個被動（追蹤指數）小類，新增「主動式 ETF」，放了兩檔真實商品：00981A 主動統一台股增長、00400A 主動國泰動能高息——都是 2023 年金管會核准的第一波主動式 ETF |
| **加一點輕鬆的視覺元素** | 桌機背景加了兩團很淡的品牌色模糊光暈（`body::before/after`，手機開啟時不會出現）、卡片圓角從 18px 加大到 20px、手機外框陰影多疊一層淡淡的品牌紅暈光、主要按鈕按下去會有一點點縮小回饋、QR 卡標題加了 📱 表情符號——都是很輕的裝飾，不影響任何版面或功能 |

**核心設計判斷**：不是直接用 logo 最鮮豔的紅（`#ED2F10`）當大面積底色——實測那個紅配白字對比度只有 4.2:1，
不夠 WCAG AA 一般文字要求的 4.5:1。改用壓深一階的紅（`#C4210A`，logo 本來就有這個深淺）當主色，
對比度提高到 5.9:1，長輩用起來字才看得清楚，也守住頁尾寫的「對比度符合 WCAG 2.1 AA」這句話。

---

### v7：主色改用指定色號、吉祥物改成官網那隻方塊角色

| 動作 | 內容 |
|---|---|
| **主色換成指定色號 `#DB190E`** | `--navy` 從 v6 的 `#C4210A` 改成使用者指定的康和標籤紅 `#DB190E`；白字對比度 5.05:1，仍過 WCAG AA。`--navy-dk` 同步重算為 `#9E120A` |
| **吉祥物「小方」改版** | 照康和官網「好康 fun 心投」活動頁那隻方塊角色重新調整：身體從通體品牌紅改成跟頭一樣的米白色，脖子加一條紅色領巾（蝴蝶結＋垂下的尾巴），眼睛放大一點更討喜；紅色只留在領巾與腳，不再是整隻都紅 |
| **拿掉不再使用的 `--brand`／`--brand-dk`** | 這兩個變數原本只給吉祥物身體用，身體改米白色之後沒有地方在用，整個拿掉避免跟 `--navy` 混淆（兩組「品牌紅」數值本來就對不上） |
| **降低「太紅」的壓迫感** | 桌機背景裝飾光暈的不透明度從 `.16` 調到 `.10`；手機外框那圈品牌色暈光也調淡；營業員視角的漸層原本尾端接近全黑（`#450C05`），改成柔和一點的暗棕紅（`#6B1C12`），整體看起來更沉穩、不那麼刺眼 |
| **logo 再次確認沒有動** | 使用者提供康和官網「好康 fun 心投」頁面截圖核對，確認目前內嵌的 logo（紅色地球圖案＋黑字「康和證券集團」）本來就是官方紅黑配色、沒有被改色，不需要更換 |

---

### v8：改用「好康 fun 心投」的配色邏輯，不再滿版紅

前一版把主色換成紅之後，畫面上同時有紅色標題列、紅色金額卡、紅色按鈕，
整頁看起來刺眼。這一版照康和「好康 fun 心投」官網的做法重新分配顏色：
**暖米底 ＋ 白卡 ＋ 紅／金／青三色輪流點綴**，紅色只留給最重要的動作。

| 動作 | 內容 |
|---|---|
| **標題列改白底＋原版 logo** | 原本是紅底白字配「純白反白版」logo。改成白底深字、底部一條紅細線，logo 換回官方紅黑原版（跟參考圖一致）。白色版 logo 整個刪掉，單檔版本反而少 25 KB |
| **金額卡不再是滿版紅** | `.hero` 從實色紅底白字，改成白底卡片＋左側紅色條＋紅色數字，紅色變點綴而不是背景。營業員視角同款白卡，色條改青色做區隔 |
| **按鈕依用途分三色** | 紅＝真正會成交的動作（確認送出、預約營業員）；金＝探索與試算（選方案、試算並申購、看詳情）；青＝聯絡人（撥電話、預約回電）。原本卡片內的按鈕全是紅的，佔了畫面上大部分的紅色面積 |
| **冷灰換暖米** | 26 處藍灰色（`#F4F8FA`、`#F0F4F7` 等）換成暖米色，並收斂成 `--tint`／`--tint-2`／`--line-2`／`--track` 四個變數。冷灰配熱紅是視覺打架的主因之一，文字色也從藍灰 `#1C2B36` 改成暖褐 `#33251C` |
| **順手修掉既有的對比度問題** | `--gold`（`#D98A1F`）原本同時當填色與文字色用，當文字放在白卡上只有 2.76:1，過不了 WCAG AA——這是**原本就有**的問題。拆成 `--gold`（亮金 `#E9A227`，只填色）與 `--gold-tx`（深琥珀 `#8A5510`，只當文字），金底按鈕改用深褐字（6.79:1），共 14 處 |
| **中性說明不再用紅字** | 「本商品不配息／保障型商品」這類中性說明原本是紅字，看起來像警告，改成一般文字色 |

**踩到的一個坑**：`background:no-repeat center/contain` 這種簡寫會把 `background-image`
重設成 `none`。把 logo 圖抽成共用規則之後，兩個 `.page-logo`／`.bar-logo` 的簡寫寫在後面，
直接把圖蓋掉了，標題列的 logo 整個消失。改成長寫（`background-repeat` / `background-position`
/ `background-size`）才修好。

---

### v9：背景全白、標題列灰底、內文改回深藍

v8 把紅色從「背景」降級成「點綴」之後仍然偏紅，因為標題、金額、每一個小標
都還是紅字。這一版把紅色徹底限縮在「品牌識別與行動」。

| 動作 | 內容 |
|---|---|
| **桌機背景改全白** | `--bg` 從暖米 `#F8F3E9` 改成純白，並移除 v6 加的兩團背景裝飾光暈（會把白色染成淡紅／淡黃）。手機外框那圈品牌紅暈光也改回中性深色投影——在純白底上那圈粉紅很明顯 |
| **手機內：上灰下白** | 標題列與狀態列改成灰底 `--tint-2`，內容區維持白卡，層次一眼分得出來 |
| **內文不再用紅字** | 新增 `--head:#0F4C75`（深藍，這是原型最早的主色）。22 處原本用紅色的標題、金額、小標全部改用深藍，白底對比度 9.09:1。紅色只留給 logo、選中的頁籤／導覽、主要按鈕、金額卡色條 |
| **青色按鈕改白字** | 「預約王專員回電」原本是青底深字，看不太清楚；改成青底白字（4.93:1，過 AA） |
| **色票整體轉回中性灰** | v8 為了配暖紅把灰階全換成暖米色，背景改白之後暖米反而顯髒，`--line`／`--tint`／`--track` 等改回中性灰藍 |

---

### v10：主色調收斂成「灰、白、深藍」，顏色只留給有意義的東西

前幾版一直在調紅色的比例，但問題不在深淺而在「顏色種類太多」。這一版
把介面主色整個換成深藍，紅色從介面上退場，只保留在 logo 與吉祥物。

| 動作 | 內容 |
|---|---|
| **主色從紅換成深藍** | `--navy` 從品牌紅 `#DB190E` 改成深藍 `#0F4C75`。所有原本紅色的按鈕、選中頁籤、導覽、卡片色條全部變深藍，全站只用一種行動色 |
| **底色改灰** | `--bg` 從純白改成灰 `#E9ECEF`，手機螢幕底 `--tint`、標題列 `--tint-2` 也是同一組灰階 |
| **金色退回原本的用途** | 金色不再用在按鈕。只留在 ① 月曆的入帳金點，以及「把選定商品加進月曆」的模擬狀態——這正是金色一開始的用途 |
| **商品卡的金額框改綠色** | 每張商品卡都有一個「每月約領 X 元」的框，原本是金色，改成綠色（錢進來＝正向，跟「適合您」同一組語意） |
| **吉祥物維持紅色** | 新增 `--brand-red` 專門給小方的領巾與鞋，介面主色改藍之後角色不會跟著變藍 |
| **PWA 主題色同步** | `theme-color`、`manifest.json`、三張 App 圖示背景一起改成深藍 |

**保留的語意色**：警示（琥珀）、高風險（酒紅）、安全／正向（綠）、聯絡真人（青）。
這些顏色都在傳達意思，不是裝飾——例如 RR4 的琥珀跟 RR5 的酒紅，長輩靠顏色就能
分出風險高低，拿掉反而會失去資訊。

---

### v11：所有「錢」的數字統一用金色

v10 把介面收成灰白深藍之後，金額數字散在各處用不同顏色（首頁深藍、
商品卡綠色），沒有一致的規則。這一版統一：**只要是錢，就是金色。**

| 位置 | 原本 | 現在 |
|---|---|---|
| ① 預計入帳的大數字與左色條 | 深藍 | 金色 |
| ② 方案卡「需投入本金」 | 深藍 | 金色 |
| ④ 商品卡「每投入 100 萬 每月約領 X 元」 | 綠色（v10 改的） | 金色 |
| ④ 申購試算「每月約領／一年約領」 | 綠色 | 金色 |

金色文字一律用 `--gold-tx`（`#8A5510`），在白卡 6.2:1、淺金底 5.49:1、
淺灰底 5.67:1，都過 WCAG AA。綠色改回只代表「安全／適合您」的狀態，
不再兼任金額顏色。

---

### v12：一般狀態白卡藍字、模擬狀態金卡白字

v11 讓所有金額都變金色之後，① 的「一般」與「模擬後」兩種狀態長得幾乎一樣，
分不出來。這一版把兩者拉開對比：

| 狀態 | 樣子 |
|---|---|
| 未含模擬 | 白卡 ＋ 深藍左色條 ＋ 深藍數字（安靜） |
| 已含模擬 | **金色實底 ＋ 白字 ＋ 亮金外框**（跳出來） |

`② 需投入本金` 也改回深藍。④ 商品卡的「每月約領」與申購試算金額維持金色。

**用亮金底＋深褐字，不是白字**：亮金 `#E9A227` 配白字只有 2.17:1，過不了 WCAG AA。
要用白字就得把金色一路壓深到 `#A8680C` 以下，那樣就不亮了、失去「眼前一亮」的效果。
改成亮金漸層 `#F7C948 → #E9A227` ＋ 深褐字 `#3A2A12`，對比度 6.36～8.82:1，
金色可以維持在最亮的狀態——這也是康和「好康 fun 心投」官網那條黃色列的做法。

說明列用**半透明白**提亮，讓中間比外框淺一階（一開始用半透明黑，中間反而比旁邊深，
看起來像凹陷）。

### 順手修掉一個既有的樣式衝突

金額卡裡的說明列左邊一直有一條括號狀的深藍弧線（在 v12 之前的截圖都看得到）。
原因是**桌機右側講稿的卡片也叫 `.note`**，它的 `border-left:5px solid var(--navy)`
漏進了手機金額卡裡的 `.hero .note`——後者沒有宣告 border，就繼承到了，
再配上 `border-radius:10px` 就畫出那條弧線。加上 `border:0` 修好。

---

### v13：營業員視角的判讀性

營業員要一整天盯這幾頁，字太淡會很吃力。這一版針對判讀做調整，
但刻意不加新顏色——多加顏色反而會讓畫面變花、重點失焦。

| 動作 | 內容 |
|---|---|
| **標題欄加深一階** | 新增 `--bar:#D5DDE5`，比卡片深一階。刻意用中性灰、不上色——上了顏色會跟紅黑 logo 打架。logo 黑字在這個底上仍有 12.68:1 |
| **問候語跟著加深** | 底色變深後原本的灰只剩 4.04:1，改成 `#4A5866`（5.32:1） |
| **「本月可轉換缺口」改深藍加粗** | 原本是灰色小字，改成深藍 700 字重，比數字本身更早被看到 |
| **卡片內文從次要灰改主要色** | `.todo-x`／`.cl-sub`／`.cl-gap` 從 `--ink-2` 改 `--ink`，長時間判讀比較不吃力 |
| **時間戳記加深** | `今天`／`2 天前` 從 `#8496A3` 改 `#6B7A88`，原本太淡 |
| **關鍵數字加粗** | 預估手續費、AUM、可承接規模原本是普通文字，改成加粗（`.s` 樣式）|

**分兩層的用意**：主要數字（可承接、每月缺口）用深藍，次要但重要的數字
（預估手續費、AUM）只加粗加深、不上色。這樣營業員一眼知道先看哪個，
畫面也不會整片都是彩色。

---

### v14：金色調自然、營業員數字分兩格、背景加深

| 動作 | 內容 |
|---|---|
| **金色改成自然一點** | 模擬卡從 `#F7C948 → #E9A227`（很飽和，像螢光黃）改成 `#E3C572 → #CFA94E`——降飽和、偏金屬金／蜂蜜金。對比度反而更好（主字 6.21～8.22:1） |
| **營業員的兩個數字分兩格** | 「預估手續費收入 · 合計缺口」原本擠在同一行，會斷在奇怪的地方也分不出主從。改成兩格並排（`.kpi`），各自有標籤，窄螢幕會自動疊成上下 |
| **大背景加深一階** | `--bg` 從 `#E9ECEF` 改 `#DFE4E9`。原本頁面底色比手機螢幕內部還淺，白色手機浮不起來；加深之後層次才對 |

---

## 四、怎麼正式上線到網路上？

### 方法 A：Netlify Drop（最快，約 30 秒，免費、含 HTTPS）★推薦

1. 開啟 <https://app.netlify.com/drop>
2. 把整個 **`康和好日子App` 資料夾**直接拖曳到網頁上
3. 等幾秒，就會拿到一組網址，例如 `https://xxxx-yyyy.netlify.app`
4. 手機打開這個網址 → 就能「加到主畫面」變成 App

> 免費、不需要註冊也能先試（註冊後才能保留網址與更新）。
> 想換好記的網址，可在 Netlify 後台 Site settings → Change site name。

### 方法 B：GitHub Pages（適合要做版本控制）

1. 在 GitHub 建立一個新的 repository
2. 把資料夾內所有檔案上傳
3. Settings → Pages → Source 選 `main` 分支、根目錄 `/`
4. 幾分鐘後即可透過 `https://你的帳號.github.io/專案名/` 開啟

### 方法 C：Cloudflare Pages

與 Netlify 類似，同樣支援拖曳上傳，速度在台灣較快。

> 三種方法都是「靜態網站託管」，**不需要後端伺服器、不需要資料庫、完全免費**，
> 因為這個原型的所有資料都寫在 `data.js` 裡。

---

## 五、怎麼在手機上變成「真的 App」？

部署到網路上（有 HTTPS）之後：

**iPhone（Safari）**
1. 用 Safari 開啟網址
2. 點下方「分享」按鈕 <kbd>↑</kbd>
3. 選「加入主畫面」
4. 桌面出現「好日子」圖示，點開即為全螢幕、無網址列

**Android（Chrome）**
1. 用 Chrome 開啟網址
2. 右上角選單 → 「安裝應用程式」或「加到主畫面」
3. 同樣會出現 App 圖示

> ⚠️ 每次更新程式碼並重新部署後，記得把 `service-worker.js` 裡的
> `var CACHE = 'haoerzi-v5';` 版本號 **+1**（改成 `v6`、`v7`…），
> 手機才會抓到最新版本，不然會一直顯示舊的快取內容。

---

## 六、如果之後要做成真正的原生 App

目前這套 PWA 已經足以應付實習簡報與初期驗證。
若未來要上架 App Store / Google Play，有兩條路：

| 做法 | 說明 | 需要重寫嗎 |
|---|---|---|
| **Capacitor** | 把現有網頁直接包成原生 App，可上架 | 幾乎不用，最省力 |
| **React Native / Flutter** | 完全重寫成原生 App，效能與體驗最好 | 要重寫 |

實務上建議先用 PWA 驗證需求，確定方向後再評估是否投入原生開發——
這也呼應了簡報中「先做方案 A 驗證，再投入方案 B」的分階段策略。

---

## 七、簡報現場 Demo 建議

開場前，先點一下右側的 QR code 小卡讓它放大，再讓台下的人拿手機掃——這比口頭念網址快很多。

整場 demo 就照著漏斗走，不要跳著點。全程約 5 分鐘：

1. **① 現金流月曆**——開場。「其他券商 App 打開是 K 線，我們打開是這個月你能領多少。」
2. **② 我的月退俸**——把滑桿從 5.5 萬拉到 **7 萬**。
   缺口從 12,140 跳到 27,140，三張方案卡的「需投入本金」同步從 265 萬變成 **592 萬**。
   **這一下就是整個商業模式**，不用再解釋。
   順便指第三張卡的「⚠ 超過您的風險屬性」——敢講風險才有信任。
3. **按「選這個 → 看月曆會變怎樣」**（選第二張「月配息債券基金」）。
   畫面跳回 ① 月曆，整塊主視覺**由藍轉金**、總額從 42,860 變成剛好 55,000、
   月曆 15 號多一顆虛線金點。
   這一秒是**成交前的臨門一腳**：客戶不是聽到報酬率，是看到補完之後的樣子。
   （右上「取消模擬」可以還原，方便重講一次。）
4. **① 上方切到「老本撐多久」子頁**——把滑桿從 3.8 萬拉到 9 萬，畫面由綠轉紅，帶出長期焦慮。
5. **④ 投資商品**——先點 ETF 分類籤，展示 ETF 又分市值型／高股息／債券型／主題海外四小類，
   點一下某張卡下面「問小幫手」，展示它會跳到 ⑤ 直接把這一檔講白話。
   接著按任一張的「試算並申購」，拉投入金額滑桿 → 每月可領與手續費即時變動 →
   按「下一步」跳出**二次確認**（防誤觸，並重述風險等級與適合度）→ 確認送出。
   一條完整的交易動線，也順勢說明手續費收入是怎麼發生的。
6. **⑤ AI 小幫手**——點「8 月怎麼變少了？」，展示它會主動指出缺口並把人導回 ②。
   按「A 字級」切到**特大**，示範無障礙；按麥克風示範語音（見下方注意事項）。
7. **切到「營業員視角」**——收尾在這裡，不要收在客戶端。
   今日待辦：5 位客戶的缺口加總換算成 **1,382 萬可承接規模、20.7 萬預估手續費**。
   我的客戶：名單不是「65 歲以上、AUM 500 萬以上」猜出來的，是「他昨晚看了月配息基金 3 分鐘」。
   客戶詳情：時間軸就是銷售腳本。
   最後一句：**客戶問了什麼，就是最真實的名單。**

> **語音示範的注意事項**
> 瀏覽器原生語音辨識需要 **HTTPS 或 localhost**，且會跳出麥克風權限詢問。
> 用 `file://` 雙擊開啟、或用單檔版本時會自動退回「模擬問答」（按下去仍會有反應，
> 只是不會真的聽），簡報現場想穩就用這個模式。
> 「念給我聽」的朗讀功能則在任何情況下都能用。

---

## 八、注意事項

- 本原型所有數據與商品皆為**模擬示意**，非真實帳戶資料或實際商品條件，簡報時請明確標示
- 未串接任何後端 API，無任何個資與交易功能
- 若正式開發，需依金管會規範完成資安檢測（源碼檢測、滲透測試、行動應用 App 基本資安檢測）
- **高齡客戶適合度**：畫面 ② 與 ④ 的 RR 標籤僅為原型示意。實際上線須依「證券商辦理財富管理業務應注意事項」與境外結構型商品相關規範，完成高齡客戶加強版適合度評估、商品說明錄音錄影與審閱期等程序
- **AI 回覆的法遵界線**：小幫手只做「查詢」與「白話翻譯」，不對個別商品做推介或適配判斷，避免觸及投顧業務範圍。畫面下方的免責聲明（`AI_DISCLAIMER`）請勿移除

---

*最後更新：2026 年 8 月*
